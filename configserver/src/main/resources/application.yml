server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/joon36/microservice-demo
          search-paths: config/hello, config/eurekaserver, config/world